// Auto-generated wrapper for contract 'SolomonDeal'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/types"

/// Storage `Storage` is defined in `@contracts/types`
struct SolomonDeal {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun SolomonDeal.fromStorage(
    storage: Storage,
    toShard: AddressShardingOptions? = null,
): SolomonDeal {
    val stateInit = ContractState {
        code: build("SolomonDeal"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return SolomonDeal { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun SolomonDeal.fromAddress(address: address): SolomonDeal {
    return SolomonDeal { address };
}

/// Deploys the contract to the blockchain
fun SolomonDeal.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun SolomonDeal.sendMakeDeal(
    self,
    from: address,
    id: uint128,
    producer: address,
    producerResponsibility: coins,
    expirationTime: uint64,
    amount: coins,
    fee: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MakeDeal { id, producer, producerResponsibility, expirationTime, amount, fee },
    });
    return net.send(from, genericMsg);
}

fun SolomonDeal.sendJudgeDeal(
    self,
    from: address,
    id: uint128,
    isProducerFault: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JudgeDeal { id, isProducerFault },
    });
    return net.send(from, genericMsg);
}

fun SolomonDeal.sendPayResponsibility(
    self,
    from: address,
    id: uint128,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: PayResponsibility { id },
    });
    return net.send(from, genericMsg);
}

fun SolomonDeal.sendProducerGiveUpDeal(
    self,
    from: address,
    id: uint128,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ProducerGiveUpDeal { id },
    });
    return net.send(from, genericMsg);
}

fun SolomonDeal.sendConsumerReturnMoneyDueToNotLockedResponsibility(
    self,
    from: address,
    id: uint128,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ConsumerReturnMoneyDueToNotLockedResponsibility { id },
    });
    return net.send(from, genericMsg);
}

fun SolomonDeal.sendCompleteTheDeal(
    self,
    from: address,
    id: uint128,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CompleteTheDeal { id },
    });
    return net.send(from, genericMsg);
}

fun SolomonDeal.sendSystemFailureWithdraw(
    self,
    from: address,
    id: uint128,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SystemFailureWithdraw { id },
    });
    return net.send(from, genericMsg);
}

fun SolomonDeal.sendChangeMinimalDealAmount(
    self,
    from: address,
    minimalDealAmount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ChangeMinimalDealAmount { minimalDealAmount },
    });
    return net.send(from, genericMsg);
}

fun SolomonDeal.sendChangeFeePart(
    self,
    from: address,
    feePart: uint8,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ChangeFeePart { feePart },
    });
    return net.send(from, genericMsg);
}

fun SolomonDeal.sendChangeSystemFailureFreezeTime(
    self,
    from: address,
    systemFailureFreezeTime: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ChangeSystemFailureFreezeTime { systemFailureFreezeTime },
    });
    return net.send(from, genericMsg);
}

fun SolomonDeal.sendChangeMinimalContractBalance(
    self,
    from: address,
    minimalContractBalance: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ChangeMinimalContractBalance { minimalContractBalance },
    });
    return net.send(from, genericMsg);
}

fun SolomonDeal.sendWithdrawSafe(
    self,
    from: address,
    tail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WithdrawSafe { tail },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun SolomonDeal.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun SolomonDeal.getDeal(self, id: uint128): Deal {
    return net.runGetMethod(self.address, "getDeal", [id]);
}

fun SolomonDeal.balance(self): coins {
    return net.runGetMethod(self.address, "balance");
}

fun SolomonDeal.feeFund(self): coins {
    return net.runGetMethod(self.address, "feeFund");
}

fun SolomonDeal.minimalDealAmount(self): coins {
    return net.runGetMethod(self.address, "minimalDealAmount");
}

fun SolomonDeal.feePart(self): uint8 {
    return net.runGetMethod(self.address, "feePart");
}

fun SolomonDeal.systemFailureFreezeTime(self): uint64 {
    return net.runGetMethod(self.address, "systemFailureFreezeTime");
}

fun SolomonDeal.minimalContractBalance(self): coins {
    return net.runGetMethod(self.address, "minimalContractBalance");
}

fun SolomonDeal.totalClientMoney(self): coins {
    return net.runGetMethod(self.address, "totalClientMoney");
}

fun SolomonDeal.judgeSystem(self): address {
    return net.runGetMethod(self.address, "judgeSystem");
}
